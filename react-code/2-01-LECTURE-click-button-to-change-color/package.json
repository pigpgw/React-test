{
  "name": "2-01-lecture-click-button-to-change-color",
  "version": "1.0.0",
  "description": "1. If you don't have a project yet, use the `vite-starter` code at the top level of this repo as a base. **Be sure to run** `npm install` in that directory. 1. Overwrite the `src` directory in your project with the `src` directory here.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
